# Database Configuration: Azure SQL Database (Primary) / H2 (Fallback)
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:testdb}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
# spring.datasource.driverClassName=${SPRING_DATASOURCE_DRIVER:org.h2.Driver}
# spring.jpa.properties.hibernate.dialect=${SPRING_JPA_DIALECT:org.hibernate.dialect.H2Dialect}

# Azure SQL Database Configuration

spring.datasource.url=jdbc:sqlserver://coursedbserver.database.windows.net:1433;database=coursedb;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30
spring.datasource.username=dovydas@coursedbserver
spring.datasource.password=Dovis0926!!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.data.rest.base-path=/api
server.port=8080